version: "3.9"

services:
  campus-qa:
    build: .
    container_name: campus-qa
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./data:/app/data
    restart: unless-stopped
